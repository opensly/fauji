// Example DOM helpers for Fauji (ESM)
// (This file is a placeholder for DOM-related matchers or helpers)

export function isDomAvailable() {
  return typeof window !== 'undefined' && typeof document !== 'undefined';
} 